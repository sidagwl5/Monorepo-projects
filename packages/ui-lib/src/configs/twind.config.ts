import { setup } from 'twind';

setup({
  theme: {
    extend: {
      colors: {
        SecondaryBg: '#C8EAD3',
        WildcardBg: '#AC8EFF',
      },
      fontFamily: {
        nunitoSans: 'Nunito Sans',
      },
    },
  },
});
