{
  "name": "image-editor-demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/image-editor-demo",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/image-editor-demo",
        "outputPath": "dist/apps/image-editor-demo"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/image-editor-demo"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "image-editor-demo:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "image-editor-demo:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "image-editor-demo:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "image-editor-demo:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/image-editor-demo/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "apps/image-editor-demo/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
